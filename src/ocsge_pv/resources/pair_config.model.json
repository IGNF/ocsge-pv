{
    "target_database": {
        "host": "host's url (domain or ip)",
        "port": 5432,
        "name": "database's name",
        "user": "user's name",
        "password": "user's password",
        "schema": "schema's name",
        "tables": {
            "detections": "detections table's name",
            "declarations": "declarations table's name",
            "links": "links table's name"
        }
    }
}