name: "🏷 PR Labeler"
on:
  - pull_request

jobs:
  labeler:
    permissions:
      issues: write
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
    - uses: actions/labeler@v5